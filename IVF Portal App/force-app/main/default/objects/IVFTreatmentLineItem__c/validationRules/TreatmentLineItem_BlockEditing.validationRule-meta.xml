<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>TreatmentLineItem_BlockEditing</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
  OR(
    ISCHANGED(TreatmentCost__c),
    ISCHANGED(TreatmentDate__c),
    ISCHANGED(TreatmentComponent__c),
    ISCHANGED(Name)
  ),
  ParentId__r.FundingComplete__c = TRUE,
  AND(
    $Profile.Name &lt;&gt; &quot;System Administrator&quot;,
    NOT($User.IVF_Finance_Controls__c)
  )
)</errorConditionFormula>
    <errorMessage>You can not edit a treatment line item because funding is marked complete.</errorMessage>
</ValidationRule>
